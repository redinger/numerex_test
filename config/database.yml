# MySQL (default setup).  Versions 4.1 and 5.0 are recommended.
#
# Install the MySQL driver:
#   gem install mysql
# On MacOS X:
#   gem install mysql -- --include=/usr/local/lib
# On Windows:
#   gem install mysql
#       Choose the win32 build.
#       Install MySQL and put its /bin directory on your path.
#
# And be sure to use new-style password hashing:
#   http://dev.mysql.com/doc/refman/5.0/en/old-client.html
development:
  adapter: mysql
  database: ublip_v2_development
  username: ublip_dev
  password: ublip_dev
  socket: /tmp/mysql.sock

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: ublip_v2_test
  username: ublip_dev
  password: ublip_dev
  socket: /tmp/mysql.sock

# These are ignored when deployed as the deployment recipe creates
# a symbolic link from the shared config to this file These are here
# so we have record of our production and staging database logins

# Production
production:
  adapter:   mysql
  database:  ublip_prod
  username:  ublip_db
  password:  pr1c7lic6
  host:      mysql50-2

# Staging
production:
  adapter:   mysql
  database:  ublip_stage
  username:  ublip_db
  password:  pr1c7lic6
  host:      mysql50-2